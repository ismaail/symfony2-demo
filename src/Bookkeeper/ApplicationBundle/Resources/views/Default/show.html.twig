{% extends 'base.html.twig' %}

{% block title %}{{ book.title }} | Bookkeeper{% endblock %}
{% block body %}
    <h1>{{ book.title }}</h1>
    <p>Number of pages: {{ book.pages }}</p>
    <p>{{ book.description }}</p>
    <p><a href="{{ path('book_edit', {'slug': book.slug}) }}">Edit</a></p>
{% endblock %}